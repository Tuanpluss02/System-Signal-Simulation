function [kq,mi] = cal(N, f, a,b)
    h = abs(b-a)/N;
    kq = 0;
    idx = 1;
    mn = 1e9;
    mx = -1e9;
    while (idx <= N)
        x = a + (idx - 1/2)*h;
        fx = f(x);
        kq = kq + fx;
        idx = idx + 1;
    end
    kq = kq *h;
end